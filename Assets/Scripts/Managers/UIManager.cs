using Fusion;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace GNW2.UIManager
{
    public class UIManager : MonoBehaviour
    {
        /*public Button hostGameButton;
        public Button joinGameButton;
        public GameObject UICanvas;

        [SerializeField] GNW2.GameManager.GameManager gameManager;

        private void Start()
        {
            if (hostGameButton != null)
            {
                hostGameButton.onClick.AddListener(() =>
                {
                    gameManager.StartGame(GameMode.Host);
                    HideUI();
                });
            }

            if (joinGameButton != null)
            {
                joinGameButton.onClick.AddListener(() =>
                {
                    gameManager.StartGame(GameMode.Client);
                    HideUI();
                });
            }
        }

        void HideUI()
        {
            if (UICanvas != null)
            {
                UICanvas.SetActive(false);
            }
        }*/

    }
}
